name: Plugin Unit Tests

on:
  push:
    branches:
      - main
      - '4.x'
  pull_request:
    branches:
      - main
      - '4.x'
  schedule:
    # Run daily at 08:00 UTC on active branches
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  unit-tests:
    uses: logstash-plugins/.ci/.github/workflows/unit-tests.yml@1.x
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/4.x' }}
    with:
      timeout-minutes: 60
